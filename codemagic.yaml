workflows:
  default-workflow:
    name: Default Workflow

    environment:
      flutter: 3.27.2

    scripts:
      - flutter --version
      - flutter clean
      - flutter pub get
      - flutter gen-l10n
      - flutter build apk --release